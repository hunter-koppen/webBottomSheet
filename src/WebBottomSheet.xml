<?xml version="1.0" encoding="utf-8"?>
<widget id="kobeon.webbottomsheet.WebBottomSheet" pluginWidget="true" needsEntityContext="true" offlineCapable="true" supportedPlatform="Web"
    xmlns="http://www.mendix.com/widget/1.0/"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://www.mendix.com/widget/1.0/ ../node_modules/mendix/custom_widget.xsd">
    <name>Web Bottom Sheet</name>
    <description/>
    <properties>
        <propertyGroup caption="General">
            <property key="openBoolean" type="attribute" required="true">
                <caption>Open</caption>
                <description>Value that determines if the bottom sheet should be shown or not.</description>
                <attributeTypes>
                    <attributeType name="Boolean"/>
                </attributeTypes>
            </property>
            <property key="blocking" type="boolean" defaultValue="true">
                <caption>Blocking</caption>
                <description>Enables focus trapping of keyboard navigation, so you can't accidentally tab out of the bottom sheet and into the background. Also sets aria-hidden on the rest of the page to prevent Screen Readers from escaping as well.</description>
            </property>
            <property key="scrollLocking" type="boolean" defaultValue="true">
                <caption>Scroll Locking</caption>
                <description>iOS Safari, and some other mobile culprits, can be tricky if you're on a page that has scrolling overflow on document.body thus enabled by default, disable this if you want full control for scrolling behaviour</description>
            </property>
            <property key="content" type="widgets" required="false">
                <caption>Content</caption>
                <description>Content inside the bottom sheet</description>
            </property>
        </propertyGroup>
    </properties>
</widget>